package duelistmod.interfaces;

import com.evacipated.cardcrawl.mod.hubris.relics.TinFlute;
import com.megacrit.cardcrawl.relics.AbstractRelic;

import duelistmod.DuelistMod;

public class HubrisHelper 
{

	public static void tinFluteHandler(AbstractRelic tinFlute)
	{
		/*
		if (DefaultMod.isHubris)
		{
			if (DefaultMod.gotTinFlute && tinFlute != null) 
			{
	            DefaultMod.tinFluteCards.add(((TinFlute) tinFlute).getSavedItem());
	            if (DefaultMod.debug) { System.out.println("theDuelist:HubrisHelper:tinFluteHandler() ---> saved item: " + ((TinFlute) tinFlute).getSavedItem().name); }
	        }
			else
			{
				if (DefaultMod.debug) { System.out.println("theDuelist:HubrisHelper:tinFluteHandler() ---> problem"); }
			}
        }
        */
	}
}

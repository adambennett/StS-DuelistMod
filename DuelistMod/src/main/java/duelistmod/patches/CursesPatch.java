package duelistmod.patches;

import com.evacipated.cardcrawl.modthespire.lib.*;
import com.megacrit.cardcrawl.cards.AbstractCard;
import com.megacrit.cardcrawl.helpers.CardLibrary;
import com.megacrit.cardcrawl.random.Random;

/*
public class CursesPatch
{
    @SpirePatch(clz = CardLibrary.class, method = "getCurse")
    public static class getCurseNoArgPatch
    {
        @SpirePostfixPatch
        public static AbstractCard Postfix(AbstractCard __result)
        {
           return __result;
        }
    }

    @SpirePatch(clz = CardLibrary.class, method = "getCurse", paramtypez={ AbstractCard.class, Random.class})
    public static class getCurseTwoArgPatch
    {
        @SpirePostfixPatch
        public static AbstractCard Postfix(AbstractCard __result)
        {
        	return __result;
        }
    }

    @SpirePatch(clz = CardLibrary.class, method = "getCurse", paramtypez={ AbstractCard.class})
    public static class getCurseOneArgPatch
    {
        @SpirePostfixPatch
        public static AbstractCard Postfix(AbstractCard __result)
        {
        	return __result;
        }
    }
}
*/